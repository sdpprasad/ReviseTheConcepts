/*
1.first.js   //pass the values throw props eaily  basic way
2.passingprops  //we page thr different props at a time having different types values
                // and also about passing list and access element in the list by using map function
3. AcessArrayandObject  //access data from arrays and objects
    also access the values from one component to another compont  (like as  AcessArrayandObject and Fruit  )
 4. getting input from AcessArrayandObject component

 5.EventUseState   // in this we use single usestate and multiple usestates

 6.Form  // about input with single usestate   && multiple input with single use state
       also about spread perator    ...   this operator store the what is the previous stored value in required variable
 
 */